# Explanation

These pages explain why the library works the way it does: what it defends
against, how its parts fit together, and what it does not do. They link to
the {doc}`../reference/index` for exact behaviour rather than restating it.

```{toctree}
:maxdepth: 1

security-model
architecture
limitations
comparison
```
